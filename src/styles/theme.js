import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Neue Metana",
    body: "Helvetica Neue",
    logo: "Product Sans",
  },
});

export default theme;
